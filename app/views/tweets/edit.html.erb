
<div class="contents row">
  <div class="container">
     <div class="contents row">
    
      <h4>
        Blogコメントの編集
      </h4>
      <%# <%= form_for :@tweet, local: true, url: {action: 'update'} do |f| %>  
       <%= form_with model: @tweet, local: true do |f| %> 
        <%= f.label :image %><br />
        <%= f.text_field :image, placeholder: "Image Url" %>

        <%= f.label :text %>
        <%= f.text_field :text, cols: "30", rows: "10"%>

      
        <button class="btn aves-effect waves-light" type="submit" name="action">更新する
        </button>

      <% end %>
    </div>
  </div>
</div>
